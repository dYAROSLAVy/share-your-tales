import { StyleSheet } from "react-native";

export const createStyles = () => {
  const styles = StyleSheet.create({
    root: {
      position: "absolute",
      right: 16,
      bottom: 98,
    },
  });
  return styles;
};
